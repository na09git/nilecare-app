import 'package:flutter/material.dart';

class DoctorVideosPage extends StatelessWidget {
  const DoctorVideosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Doctor Videos')),
      body: const Center(child: Text('Doctor Videos')),
    );
  }
}
