import 'package:flutter/material.dart';

class PatientInboxPage extends StatelessWidget {
  const PatientInboxPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Patient Inbox'));
  }
}
