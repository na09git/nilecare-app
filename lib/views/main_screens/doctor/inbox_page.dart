import 'package:flutter/material.dart';

class DoctorInboxPage extends StatelessWidget {
  const DoctorInboxPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Doctor Inbox'));
  }
}
